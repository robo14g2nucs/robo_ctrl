#ifndef _robo_globals_
#define _robo_globals_

//	Link to this file should be saved at ~/catkin_ws/devel/include/robo_globals.h

#include <cmath>

static const double WHEEL_BASE = 0.206;
static const double WHEEL_RADIUS = 0.0485;
static const double CTRL_FREQ = 10.0;
static const double TICKSPR = 360.0;
static const double PI = acos(-1.0);
static const double TWOPI = 2*PI;
static const double IR_SIDE_SENSOR_DISTANCE = 0.19;

#endif
